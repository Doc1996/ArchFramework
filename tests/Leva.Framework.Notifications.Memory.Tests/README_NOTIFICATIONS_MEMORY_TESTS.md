# Leva.Framework.Notifications.Memory.Tests

`Leva.Framework.Notifications.Memory.Tests` verifies the in-memory provider implementation of `Leva.Framework.Notifications`. These tests cover memory gateway capture, entry storage, loading, deletion, clearing, and service group composition.

## Coverage

`MemoryNotificationTests` - Verifies in-memory sending, storing, loading, deleting, clearing, and service group composition.
